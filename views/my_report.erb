<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="description" content="">
    <meta name="author" content="">

    <title>Badger</title>

    <script src="http://d3js.org/d3.v3.min.js"></script>
    <script src="js/RadarChart.js"></script>
        <style type="text/css">
      .my-container [class^="col-"] {
          padding-top: 10px;
          padding-bottom: 10px;
          background-color: rgba(255,255,255,.0);
          border: 1px dashed rgba(0,0,0,.4);

          .my-inner {
            padding: 5px;
            background-color: rgba(255,255,255,.0);
            border: 1px dashed rgba(0,0,0,.4);
  }
      }
      #coinId{
         display:inline-block;
      }
    </style>

    <script type="text/JavaScript">
    //<![CDATA[
    //global variables

    priceOfUnlock = 300;
    function UnlockBtn(btnParentId, btnId, uuid, index, coinId, author,tester){
       this.btnParent = document.getElementById(btnParentId);
       this.btn = document.getElementById(btnId);
       this.coinLabel = document.getElementById(coinId);
       this.recordUUID = uuid;
       this.recordIndex = index;
       this.author = author;
       this.tester = tester;
       var obj = this;
       this.btn.onclick = function(){obj.btnClicked();}    
    }
    UnlockBtn.prototype.btnClicked = function(){
       var obj = this;
       var coinsNum = parseInt(obj.coinLabel.innerHTML);
       console.log(obj.coinLabel.innerHTML)
       console.log(coinsNum)
       if(coinsNum < priceOfUnlock){
           window.alert("sorry, not enough coins...");
           return;
       }
       obj.coinLabel.innerHTML = (coinsNum - priceOfUnlock).toString();
       obj.btnParent.removeChild(obj.btn);
       var authorTextNode = document.createTextNode(obj.author);
       obj.btnParent.appendChild(authorTextNode);
       //send request
       var myXHR = new XMLHttpRequest();
       //myXHR.open("GET", "/unlock");
       myXHR.open("POST", "/unlock" + "?tester=" + obj.tester + "&uuid=" + obj.recordUUID + "&index=" + obj.recordIndex);
       myXHR.onreadystatechange = function() {
           if (this.readyState != 4) {
              return;
           }
           if (this.status == 200) {
              return;
           } else {
             console.log("error occurred!");
           }
       }
       myXHR.send();
    }

    // Unlock guesser
    priceOfUnlockGuesser = 300;
    function UnlockGuesserBtn(btnParentId, btnId, uuid, coinId, author){
       this.btnParent = document.getElementById(btnParentId);
       this.btn = document.getElementById(btnId);
       this.coinLabel = document.getElementById(coinId);
       this.uuid = uuid;
       this.author = author;
       var obj = this;
       this.btn.onclick = function(){obj.btnClicked();}    
    }

    UnlockGuesserBtn.prototype.btnClicked = function(){
       var obj = this;
       var coinsNum = parseInt(obj.coinLabel.innerHTML);
       console.log(obj.coinLabel.innerHTML)
       console.log(coinsNum)
       if(coinsNum < priceOfUnlockGuesser){
           window.alert("sorry, not enough coins...");
           return;
       }
       obj.coinLabel.innerHTML = (coinsNum - priceOfUnlockGuesser).toString();
       obj.btnParent.removeChild(obj.btn);
       var authorTextNode = document.createTextNode(obj.author);
       obj.btnParent.appendChild(authorTextNode);
       //send request
       var myXHR = new XMLHttpRequest();
       myXHR.open("POST", "/unlockGuesser" + "?uuid=" + obj.uuid);
       myXHR.onreadystatechange = function() {
           if (this.readyState != 4) {
              return;
           }
           if (this.status == 200) {
              return;
           } else {
             console.log("error occurred!");
           }
       }
       myXHR.send();
    }
    //]]>
    </script>

    <!-- Bootstrap core CSS -->
    <!-- Latest compiled and minified CSS -->
    <link rel="stylesheet" href="//netdna.bootstrapcdn.com/bootstrap/3.0.3/css/bootstrap.min.css">

    <!-- Optional theme -->
    <link rel="stylesheet" href="//netdna.bootstrapcdn.com/bootstrap/3.0.3/css/bootstrap-theme.min.css">

  </head>
<!-- NAVBAR
================================================== -->
  <body>
  <div class="container marketing" style="background-color: #66CCFF;">
    <div class="row">
      <div class="col-xs-12 col-sm-12"><center><h1><font color="white">My Report</font></h1></div>
    </div>
  </div>

  <!-- Nav tabs -->
<ul class="nav nav-tabs" role="tablist">
  <li class="active"><a href="#spiderweb" role="tab" data-toggle="tab">SpiderWeb</a></li>
  <li><a href="#author" role="tab" data-toggle="tab">Author</a></li>
  <li><a href="#guesser" role="tab" data-toggle="tab">Guesser</a></li>
</ul>

<!-- Tab panes -->
<div class="tab-content">
  <div class="tab-pane" id="spiderweb">
    <div id="body">
    <div id="chart" ></div>
    </div>
 
    <script type="text/JavaScript">
    //<![CDATA[
var uagent = navigator.userAgent.toLowerCase();

var w, h;
var isIphone;
if( screen.width <= 480 ) {
    //is iphone
    w = 180;
    h = 180;
    isIphone = true;
}else{
    chartelement = document.getElementById("chart");
    chartelement.style.paddingLeft = "250px";
    w = 600;
    h = 600;
    isIphone = false;
}

var colorscale = d3.scale.ordinal()
    .domain(["foo", "bar", "baz"])
    .range(["#66C4C1","#F57CD5","#333"]);

//Legend titles
var LegendOptions = ['<%=@time_difference%>','New'];

//Data
var d = [
      [
      {axis:"Relationship",value:<%=@oldR%>},
      {axis:"Professional",value:<%=@oldP%>},
      {axis:"Social",value:<%=@oldS%>}
      ],[
      {axis:"Relationship",value:<%=@newR%>},
      {axis:"Professional",value:<%=@newP%>},
      {axis:"Social",value:<%=@newS%>}
      ]
    ];

//Options for the Radar chart, other than default
var mycfg = {
  w: w,
  h: h,
  maxValue: 0.6,
  levels: 6,
  ExtraWidthX: 300,
  svgWidth:(isIphone)? 320: 700,
  svgHeight:(isIphone)? 280: 600
}

//Call function to draw the Radar chart
//Will expect that data is in %'s
RadarChart.draw("#chart", d, mycfg);

////////////////////////////////////////////
/////////// Initiate legend ////////////////
////////////////////////////////////////////

var svg = d3.select('#body')
  .selectAll('svg')
  .append('svg')
  .attr("width", w+300)
  .attr("height", h+300)

//Create the title for the legend
var text = svg.append("text")
  .attr("class", "title")
  .attr('transform', 'translate(90,0)') 
  .attr("x", w - 70)
  .attr("y", 10)
  .attr("font-size", "12px")
  .attr("fill", "#404040")
  .text("");
    
//Initiate Legend 
var legend = svg.append("g")
  .attr("class", "legend")
  .attr("height", 100)
  .attr("width", 200)
  .attr('transform', 'translate(90,20)') 
  ;
  //Create colour squares
  legend.selectAll('rect')
    .data(LegendOptions)
    .enter()
    .append("rect")
    //change x offset
    .attr("x", w - 190)
    .attr("y", function(d, i){ return i * 20 + 200;})
    .attr("width", 10)
    .attr("height", 10)
    .style("fill", function(d, i){ return colorscale(i);})
    ;
  //Create text next to squares
  legend.selectAll('text')
    .data(LegendOptions)
    .enter()
    .append("text")
    //change x offset
    .attr("x", w - 177)
    .attr("y", function(d, i){ return i * 20 + 209;})
    .attr("font-size", "18px")
    .attr("fill", "#737373")
    .text(function(d) { return d; })
    ; 
        //]]>
    </script>
  </div>
  <div class="tab-pane" id="author">
    
    <!-- Marketing messaging and featurettes
    ================================================== -->
    <!-- Wrap the rest of the page in another container to center all the content. -->


    <div class="col-xs-12 col-sm-12"  style="background-color: #66CCFF;" >
      <center><font color="white" size="3pt">Spend <img src="/img/coin.jpg" width="17" height="17"> 300 to unlock an author</font></center>
    </div>

    <div class="col-xs-12 col-sm-12" style="background-color: #66CCFF;">
      <center> 
        <img src="/img/coin.jpg" width="22" height="22">
        <font color="white"><div id="coinId"><%= @@coins[session[:tester]] %></div></font>
      </center>
    </div>
</br>

<div class="container-marketing">

  <%@my_questions.each do |question|%>
    <%index_array = Array.new%>
    <%index_array << question[0].to_s%>
    <%index_array << question[1].to_s%>
    <%quiz = question[3]%>
    <%found = @@unlocked_uuid_index[session[:tester]] && @@unlocked_uuid_index[session[:tester]].include?(index_array)%>
    <div class="container my-container">
      <font color="orange" size="5pt">
        <span class="glyphicon glyphicon-user"></span>
        <%if found%>
           <%=question[2]%>
        <%else%>
           <%divID = "authorDiv" + index_array[0].to_s + index_array[1].to_s%>
           <%btnID = "authorBtn" + index_array[0].to_s + index_array[1].to_s%>
           <div class="inline_form" id=<%=divID%> >
                    <button id=<%=btnID%> class="btn btn-warning">Unlock</button>
           </div>
           <script type="text/JavaScript">
                 //<![CDATA[
                    console.log("here")
                    new UnlockBtn("<%=divID%>", "<%=btnID%>", "<%=index_array[0].to_s%>", "<%=index_array[1].to_s%>","coinId", "<%=question[2]%>","<%=session[:tester]%>"); 
                 //]]>
           </script>
        <%end%>
      </font>
      <div class="row">
        <div class="col-xs-12 col-sm-8"><center><%=quiz["question"]%></center></div>
      </div>
      <div class="row">
        <div class="col-xs-6 col-sm-4"><center>
          <%if quiz["option0"] == quiz["answer"]%>
             <span class="glyphicon glyphicon-ok"></span>
             <font>
          <%else%>
             <font color="#CCCCCC">
          <%end%>
          <%if quiz["option0"] == session[:tester]%>
             You
          <%else%>
             <%=quiz["option0"]%>
          <%end%>
          </font>
          </center>
        </div>
        <div class="col-xs-6 col-sm-4"><center>
          <%if quiz["option1"] == quiz["answer"]%>
             <span class="glyphicon glyphicon-ok"></span>
             <font>
          <%else%>
             <font color="#CCCCCC">
          <%end%>
          <%if quiz["option1"] == session[:tester]%>
             You
          <%else%>
             <%=quiz["option1"]%>
          <%end%>
          </font>
          </center>
        </div>
      </div>
    </div>
    <br>
  <%end%>
  </div>
</div>
  <div class="tab-pane" id="guesser">
    <div class="container-marketing">
      
      <%@guesser_questions.each do |question|%>
        <%quiz  = question["quiz"]%>
        <%found = @@librarian.is_guesser_unlocked?(session[:tester], quiz["uuid"])%>

        <div class="container my-container">
          <font color="orange" size="5pt">
            <span class="glyphicon glyphicon-user"></span>
            <%if found%>
               <%=question["guesser"]%>
            <%else%>
               <%divID = "authorDiv" + quiz["uuid"]%>
               <%btnID = "authorBtn" + quiz["uuid"]%>
               <div class="inline_form" id=<%=divID%> >
                        <button id=<%=btnID%> class="btn btn-warning">Unlock</button>
               </div>
               <script type="text/JavaScript">
                     //<![CDATA[
                        console.log("here")
                        //btnParentId, btnId, uuid, coinId, author
                        new UnlockGuesserBtn("<%=divID%>", "<%=btnID%>", "<%=quiz['uuid']%>","coinId", "<%=question['guesser']%>"); 
                     //]]>
               </script>
            <%end%>
          </font>
          <div class="row">
            <div class="col-xs-12 col-sm-8"><center><%=quiz["question"]%></center></div>
          </div>
          <div class="row">
            <div class="col-xs-6 col-sm-4">
              <center>
              <%if quiz["option0"] == quiz["answer"]%>
                 <span class="glyphicon glyphicon-ok"></span>
                 <font>
              <%else%>
                 <font color="#CCCCCC">
              <%end%>
              <%if quiz["option0"] == session[:tester]%>
                 You
              <%else%>
                 <%=quiz["option0"]%>
              <%end%>
              </font>
              </center>
            </div>
            <div class="col-xs-6 col-sm-4">
              <center>
              <%if quiz["option1"] == quiz["answer"]%>
                 <span class="glyphicon glyphicon-ok"></span>
                 <font>
              <%else%>
                 <font color="#CCCCCC">
              <%end%>
              <%if quiz["option1"] == session[:tester]%>
                 You
              <%else%>
                 <%=quiz["option1"]%>
              <%end%>
              </font>
              </center>
            </div>
          </div>
        </div>
        <br>
      <%end%>

    </div>
  </div>


</br>
    <form class="form-horizontal" role="form" action="home" method="post">
      <center><button type="submit" class="btn btn-success btn-block">OK</button></center>
    </form>

      <!-- FOOTER -->
      <footer>
        </br></br></br></br></br>
        <p>&copy; 2014 Orrzs Inc.</p>
      </footer>


    <!-- Bootstrap core JavaScript
    ================================================== -->
    <!-- Placed at the end of the document so the pages load faster -->
    
    <!-- Latest compiled and minified JavaScript -->
    <script src="https://code.jquery.com/jquery-1.10.2.min.js"></script>
    <script src="//netdna.bootstrapcdn.com/bootstrap/3.0.3/js/bootstrap.min.js"></script>    
  </body>
</html>
